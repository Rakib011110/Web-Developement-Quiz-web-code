

import React from 'react';

const Options = ({ Option }) => {
    return (
        <div>
            <card>
                <col>
                    {Option}
                </col>
            </card>
        </div>
    );
};

export default Options;